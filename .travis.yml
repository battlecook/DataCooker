sudo: required
language: php

git:
  depth: 2

php:
- 7.1
- 7.2

services:
- memcached

before_script:
- sudo apt-get update
- sudo apt-get install php-memcached
- phpenv config-add travis.php.ini
- composer install

script:
- phpunit BufferTest.php
